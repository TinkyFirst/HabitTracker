# ✅ Як перевірити що GoogleService-Info.plist додано правильно

## 🔍 Швидка перевірка (30 секунд)

### Крок 1: Знайди файл в Project Navigator

**В Xcode:**

1. Подивись в **Project Navigator** (ліва панель, ⌘+1)

2. Знайди файл **`GoogleService-Info.plist`**

3. **Де має бути:**
   ```
   TrackHabit (папка)
   ├── TrackHabitApp.swift
   ├── LanguageManager.swift
   ├── FirebaseNotificationManager.swift
   ├── GoogleService-Info.plist  ← ОСЬ ТУТ!
   └── інші файли...
   ```

❌ **НЕПРАВИЛЬНО:** Якщо файл в самому верху (поруч з синьою іконкою проекту)

✅ **ПРАВИЛЬНО:** Файл в папці з іншими .swift файлами

---

### Крок 2: Перевір Target Membership

**Клікни на файл `GoogleService-Info.plist`:**

1. В правій панелі відкриється **File Inspector** (якщо не видно — натисни ⌘+⌥+1)

2. Знайди розділ **"Target Membership"**

3. **Має бути:**
   ```
   ✅ TrackHabit
   ```

❌ **ЯКЩО ПОРОЖНЬО** або немає галочки:
- Постав галочку вручну ✅

---

### Крок 3: Перевір вміст файлу

**Клікни на файл `GoogleService-Info.plist`:**

1. В центральній панелі має показатися XML код

2. **Перевір що є:**
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
   <plist version="1.0">
   <dict>
       <key>CLIENT_ID</key>
       <string>...</string>
       <key>REVERSED_CLIENT_ID</key>
       <string>com.googleusercontent.apps...</string>
       <key>BUNDLE_ID</key>
       <string>твій.bundle.id</string>
       ...
   </dict>
   </plist>
   ```

3. **Перевір `BUNDLE_ID`:**
   - Має збігатися з Bundle ID твого проекту в Xcode!

---

## ❓ Якщо файл не там де треба

### Варіант A: Видалити і додати заново

1. **Правою кнопкою** на файл → **Delete**
2. В діалозі вибери **"Move to Trash"** (не просто Remove Reference)
3. Повтори **Крок 3.3** з головного гайду (`FIREBASE_STEP_BY_STEP.md`)

### Варіант B: Перемістити через Finder

1. В Finder знайди папку проекту
2. Перемісти `GoogleService-Info.plist` в папку з .swift файлами
3. В Xcode: **File → Add Files to "TrackHabit"**
4. Вибери файл
5. ✅ **Copy items if needed**
6. ✅ **Add to targets: TrackHabit**
7. Add

---

## 🧪 Тест: Чи файл працює?

**Запусти додаток (⌘+R):**

В консолі Xcode (⌘+⇧+C) має бути:

```
✅ [Firebase/Core][I-COR000003] The default Firebase app has not yet been configured...
```

або

```
✅ Firebase configured
```

❌ **Якщо бачиш:**
```
❌ Could not locate configuration file: 'GoogleService-Info.plist'
```

→ Файл не знайдено! Повернись до **Варіанту A** вище.

---

## 📋 Checklist

Перевір всі пункти:

- [ ] Файл `GoogleService-Info.plist` видно в Project Navigator
- [ ] Файл в папці з .swift файлами (не вгорі!)
- [ ] Target Membership: ✅ TrackHabit
- [ ] Вміст файлу містить `BUNDLE_ID` який збігається з твоїм
- [ ] При запуску додатку немає помилки "Could not locate"

---

✅ Якщо всі пункти виконані — файл додано правильно!

🆘 Якщо щось не так — напиши мені, допоможу! 💬

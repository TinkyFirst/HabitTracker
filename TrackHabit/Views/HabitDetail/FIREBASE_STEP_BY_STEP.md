# 🔥 Firebase Покрокова Інструкція

## ✅ ЩО ВЖЕ ЗРОБЛЕНО:

1. ✅ `FirebaseNotificationManager.swift` створено
2. ✅ `PushNotificationsSettingsView.swift` створено
3. ✅ UI в Settings додано
4. ✅ Локалізація готова

---

## 📋 ЩО ПОТРІБНО ЗРОБИТИ:

---

# КРОК 1: Створити Firebase Проект (5 хвилин)

## 1.1 Відкрий Firebase Console

1. Відкрий браузер (Safari, Chrome)
2. Перейди на: **https://console.firebase.google.com**
3. Увійди через Google акаунт (або створи новий)

---

## 1.2 Створи новий проект

1. Натисни велику кнопку **"Add project"** (або **"Створити проект"**)

2. **Крок 1 - Назва проекту:**
   ```
   Введи: Track Habit
   ```
   - Натисни **Continue** (Продовжити)

3. **Крок 2 - Google Analytics:**
   ```
   Можеш вимкнути перемикач (не обов'язково зараз)
   ```
   - Натисни **Create project** (Створити проект)

4. **Зачекай 30 секунд** поки проект створюється
   - Побачиш анімацію завантаження

5. Коли з'явиться **"Your new project is ready"**:
   - Натисни **Continue**

✅ Firebase проект створено!

---

# КРОК 2: Додати iOS додаток до Firebase

## 2.1 Знайди свій Bundle ID в Xcode

**В Xcode:**

1. Натисни на **назву проекту** в лівій панелі (самий верх Project Navigator)
   ```
   TrackHabit (синя іконка)
   ```

2. В центральній панелі вибери **TARGET: TrackHabit** (не Project!)

3. Перейди на таб **"General"** (вгорі)

4. В розділі **"Identity"** знайди **"Bundle Identifier"**
   ```
   Наприклад: com.yourname.TrackHabit
   ```

5. **СКОПІЮЙ цей Bundle ID** (клік правою кнопкою → Copy)
   - Або запиши на листочок 📝

---

## 2.2 Додай iOS додаток в Firebase

**Повернись в браузер (Firebase Console):**

1. На головній сторінці проекту знайди секцію:
   ```
   Get started by adding Firebase to your app
   ```

2. Натисни на **iOS іконку** (або кнопку з логотипом Apple)

3. **Форма реєстрації iOS додатку:**

   **a) Apple bundle ID:** (обов'язкове поле)
   ```
   Вклей твій Bundle ID з Xcode
   Наприклад: com.yourname.TrackHabit
   ```

   **b) App nickname:** (не обов'язково)
   ```
   Можна написати: Track Habit iOS
   або залишити порожнім
   ```

   **c) App Store ID:** (не обов'язково)
   ```
   Залиш порожнім (додаток ще не в App Store)
   ```

4. Натисни **"Register app"** (Зареєструвати додаток)

✅ iOS додаток зареєстровано!

---

# КРОК 3: Завантажити GoogleService-Info.plist

## 3.1 Завантаж файл з Firebase

**В Firebase Console (продовжуємо з попереднього кроку):**

1. Побачиш екран **"Download GoogleService-Info.plist"**

2. Натисни **синю кнопку "Download GoogleService-Info.plist"**
   ```
   Файл завантажиться в папку Downloads
   ```

3. **НЕ ЗАКРИВАЙ** вкладку браузера (ще потрібна!)

---

## 3.2 Перевір що файл завантажився

**В Finder:**

1. Відкрий папку **Downloads** (Завантаження)
2. Знайди файл: **GoogleService-Info.plist**
   - Має іконку документа з кодом
3. **НЕ ВІДКРИВАЙ** файл, просто переконайся що він є

---

## 3.3 Додай файл в Xcode (ВАЖЛИВО!)

**В Xcode:**

1. **Знайди папку проекту** в Project Navigator (ліва панель):
   ```
   TrackHabit (папка з файлами, не синя іконка вгорі)
   └── TrackHabitApp.swift
   └── LanguageManager.swift
   └── інші .swift файли...
   ```

2. **Перетягни файл `GoogleService-Info.plist`** з Finder в цю папку
   - Можна положити поруч з `TrackHabitApp.swift`

3. **ВАЖЛИВО!** Коли з'явиться діалог **"Choose options for adding these files"**:

   ✅ **ОБОВ'ЯЗКОВО постав галочки:**
   ```
   ✅ Copy items if needed (Копіювати якщо потрібно)
   ✅ Create groups (Створити групи)
   
   ✅ Add to targets:
      ✅ TrackHabit (основний додаток)
   ```

4. Натисни **"Finish"**

5. **Перевір що файл з'явився:**
   - В Project Navigator має бути `GoogleService-Info.plist`
   - Клікни на файл - в правій панелі має показати вміст (XML)
   - В правій панелі (File Inspector) в розділі **"Target Membership"** має бути ✅ TrackHabit

❗ **ТИПОВА ПОМИЛКА:** Якщо просто кинути файл десь у верхню частину проекту (синя іконка), він не додасться правильно. Обов'язково тягни в **папку з файлами** і ставь галочки!

✅ GoogleService-Info.plist додано правильно!

---

# КРОК 4: Додати Firebase SDK через Swift Package Manager

## 4.1 Відкрий Package Dependencies

**В Xcode:**

1. В верхньому меню натисни:
   ```
   File → Add Package Dependencies...
   ```
   (або Add Packages...)

2. Відкриється вікно **"Add Package Dependency"**

---

## 4.2 Додай Firebase SDK

1. В полі пошуку вгорі справа введи:
   ```
   https://github.com/firebase/firebase-ios-sdk
   ```
   (Або просто: firebase-ios-sdk)

2. Натисни **Enter** або кнопку пошуку

3. З'явиться **firebase-ios-sdk** від Apple
   - Переконайся що правильний репозиторій: `firebase/firebase-ios-sdk`

4. **Dependency Rule:**
   ```
   Up to Next Major Version: 11.0.0 
   (або Latest Version - не критично)
   ```

5. Натисни **"Add Package"** (внизу справа)

6. **Зачекай 1-3 хвилини** поки Xcode завантажує пакет
   - Побачиш прогрес бар "Resolving package dependencies..."
   - Може бути повільно - це нормально!

---

## 4.3 Вибери потрібні продукти Firebase

Коли завантаження завершиться, з'явиться вікно **"Choose Package Products"**

**ВИБЕРИ ТІЛЬКИ:**

```
✅ FirebaseMessaging (обов'язково!)
✅ FirebaseAnalytics (опціонально, для статистики)
```

**НЕ ВИБИРАЙ** інші (Auth, Firestore, тощо - не потрібні зараз)

Натисни **"Add Package"** (внизу справа)

Зачекай ще ~30 секунд поки Xcode інтегрує пакети

✅ Firebase SDK додано!

---

# КРОК 5: Налаштувати Capabilities (Push Notifications)

## 5.1 Відкрий Signing & Capabilities

**В Xcode:**

1. Натисни на **назву проекту** в Project Navigator (синя іконка вгорі)

2. Вибери **TARGET: TrackHabit**

3. Перейди на таб **"Signing & Capabilities"** (вгорі)

---

## 5.2 Додай Push Notifications

1. Натисни **"+ Capability"** (вгорі зліва, під табами)

2. В пошуку введи:
   ```
   Push Notifications
   ```

3. Клікни на **"Push Notifications"** в списку

4. Має з'явитися нова секція **"Push Notifications"** (без налаштувань всередині)

✅ Push Notifications додано!

---

## 5.3 Додай Background Modes

1. Знову натисни **"+ Capability"**

2. В пошуку введи:
   ```
   Background Modes
   ```

3. Клікни на **"Background Modes"**

4. Відкриється секція **"Background Modes"** з галочками

5. **ПОСТАВ ГАЛОЧКУ:**
   ```
   ✅ Remote notifications
   ```

✅ Background Modes налаштовано!

**Має вийти так:**
```
Signing & Capabilities
├── Signing (Debug/Release)
├── Push Notifications
└── Background Modes
    ✅ Remote notifications
```

---

# КРОК 6: Налаштувати APNs в Apple Developer

⚠️ **ПОТРІБЕН ПЛАТНИЙ Developer Account ($99/рік)**

Якщо ще НЕ МАЄш — **пропусти цей крок зараз**, Firebase буде працювати в тестовому режимі.

## 6.1 Створи APNs Authentication Key

1. Зайди на: **https://developer.apple.com/account**

2. Увійди зі своїм Apple ID

3. В лівому меню натисни:
   ```
   Certificates, Identifiers & Profiles → Keys
   ```

4. Натисни **"+"** (Create a new key)

5. **Key Name:**
   ```
   Firebase Push Key
   ```

6. **Постав галочку:**
   ```
   ✅ Apple Push Notifications service (APNs)
   ```

7. Натисни **"Continue"** → **"Register"**

8. **ВАЖЛИВО!** На наступному екрані:
   ```
   📥 Download .p8 файл (можна завантажити ТІЛЬКИ РАЗ!)
   📝 Збережи Key ID (10 символів)
   📝 Збережи Team ID (верхній правий кут)
   ```

9. Натисни **"Done"**

---

## 6.2 Завантаж ключ в Firebase

**Повернись в браузер (Firebase Console):**

1. В лівому меню натисни **⚙️ (Settings)** → **"Project settings"**

2. Перейди на таб **"Cloud Messaging"**

3. Scroll вниз до секції **"Apple app configuration"**

4. Знайди свій iOS додаток (по Bundle ID)

5. Клікни **"Upload"** (або "APNs Authentication Key")

6. **Завантаж:**
   ```
   📥 .p8 файл (з Downloads)
   Key ID: (ти записав)
   Team ID: (ти записав)
   ```

7. Натисни **"Upload"**

✅ Статус має змінитися на зелену галочку

---

# КРОК 7: Налаштувати код (Розкоментувати)

## 7.1 Відкрий FirebaseNotificationManager.swift

**В Xcode:**

1. Знайди файл `FirebaseNotificationManager.swift` в Project Navigator

2. Клікни на файл

---

## 7.2 Розкоментуй блок в configure()

**Знайди метод `configure()` (біля рядка 52):**

**БУЛО:**
```swift
func configure() {
    // РОЗКОМЕНТУЙ КОЛИ ДОДАСИ FIREBASE:
    // import Firebase
    // import FirebaseMessaging
    //
    // FirebaseApp.configure()
    // Messaging.messaging().delegate = self
    
    print("🔥 Firebase configured")
}
```

**СТАНЕ:**
```swift
func configure() {
    // РОЗКОМЕНТУЙ КОЛИ ДОДАСИ FIREBASE:
    import Firebase
    import FirebaseMessaging
    
    FirebaseApp.configure()
    Messaging.messaging().delegate = self
    
    print("🔥 Firebase configured")
}
```

❌ **СТОП!** Так не можна! import всередині методу не працює!

**ПРАВИЛЬНО - додай import ВГОРІ файлу:**

Знайди верх файлу (рядки 1-3):

**БУЛО:**
```swift
import Foundation
import UserNotifications
import UIKit
```

**ДОДАЙ:**
```swift
import Foundation
import UserNotifications
import UIKit
import Firebase
import FirebaseMessaging
```

А в методі `configure()` **ПРИБЕРИ коментарі з import**, залиш тільки:

```swift
func configure() {
    FirebaseApp.configure()
    Messaging.messaging().delegate = self
    
    print("🔥 Firebase configured")
}
```

---

## 7.3 Розкоментуй extension'и в кінці файлу

**Scroll вниз до рядка ~109 (після "MARK: - Firebase Messaging Delegate"):**

**ЗНАЙДИ:**
```swift
// MARK: - Firebase Messaging Delegate
// РОЗКОМЕНТУЙ КОЛИ ДОДАСИ FIREBASE SDK:
/*
import FirebaseMessaging

extension FirebaseNotificationManager: MessagingDelegate {
    ...
}

extension FirebaseNotificationManager: UNUserNotificationCenterDelegate {
    ...
}
*/
```

**ВИДАЛИ:**
- Рядок `import FirebaseMessaging` (import вже вгорі)
- Коментар `// РОЗКОМЕНТУЙ...`
- Символи `/*` та `*/`

**СТАНЕ:**
```swift
// MARK: - Firebase Messaging Delegate
extension FirebaseNotificationManager: MessagingDelegate {
    /// Отримуємо FCM токен
    func messaging(_ messaging: Messaging, didReceiveRegistrationToken fcmToken: String?) {
        print("🔥 Firebase token: \(fcmToken ?? "nil")")
        
        DispatchQueue.main.async { [weak self] in
            self?.fcmToken = fcmToken
            
            // Зберігаємо токен локально
            if let token = fcmToken {
                UserDefaults.standard.set(token, forKey: "fcm_token")
            }
        }
    }
}

extension FirebaseNotificationManager: UNUserNotificationCenterDelegate {
    /// Обробка повідомлення коли додаток відкритий
    func userNotificationCenter(_ center: UNUserNotificationCenter,
                                willPresent notification: UNNotification,
                                withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
        let userInfo = notification.request.content.userInfo
        print("📬 Received notification: \(userInfo)")
        
        // Показуємо повідомлення навіть коли додаток відкритий
        completionHandler([.banner, .sound, .badge])
    }
    
    /// Обробка натискання на повідомлення
    func userNotificationCenter(_ center: UNUserNotificationCenter,
                                didReceive response: UNNotificationResponse,
                                withCompletionHandler completionHandler: @escaping () -> Void) {
        let userInfo = response.notification.request.content.userInfo
        print("👆 User tapped notification: \(userInfo)")
        
        completionHandler()
    }
}
```

**Збережи файл:** `Cmd + S`

---

# КРОК 8: Створити AppDelegate.swift

## 8.1 Створи новий файл

**В Xcode:**

1. В Project Navigator **клікни правою кнопкою** на папку з файлами (де TrackHabitApp.swift)

2. **New File...** (або `Cmd + N`)

3. Вибери **Swift File**

4. Натисни **Next**

5. **Save As:**
   ```
   AppDelegate.swift
   ```

6. **Targets:**
   ```
   ✅ TrackHabit
   ```

7. Натисни **Create**

---

## 8.2 Додай код в AppDelegate.swift

**Видали весь вміст файлу і вклей:**

```swift
import UIKit
import Firebase
import FirebaseMessaging

class AppDelegate: NSObject, UIApplicationDelegate {
    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]? = nil) -> Bool {
        
        // Ініціалізуємо Firebase
        FirebaseNotificationManager.shared.configure()
        
        // Налаштовуємо delegates
        UNUserNotificationCenter.current().delegate = FirebaseNotificationManager.shared
        
        print("✅ AppDelegate initialized")
        
        return true
    }
    
    // Обробка APNs token
    func application(_ application: UIApplication,
                     didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
        print("📱 Received APNs token")
        // Firebase автоматично отримає токен
        Messaging.messaging().apnsToken = deviceToken
    }
    
    func application(_ application: UIApplication,
                     didFailToRegisterForRemoteNotificationsWithError error: Error) {
        print("❌ Failed to register for remote notifications: \(error)")
    }
}
```

**Збережи:** `Cmd + S`

---

## 8.3 Підключи AppDelegate до TrackHabitApp

**Відкрий файл `TrackHabitApp.swift`:**

**ЗНАЙДИ:**
```swift
@main
struct TrackHabitApp: App {
    @State private var selectedHabitId: UUID?
    ...
```

**ДОДАЙ РЯДОК ОДРАЗУ ПІСЛЯ `struct TrackHabitApp: App {`:**

```swift
@main
struct TrackHabitApp: App {
    @UIApplicationDelegateAdaptor(AppDelegate.self) var appDelegate  // ← ДОДАЙ ЦЕЙ РЯДОК
    
    @State private var selectedHabitId: UUID?
    @AppStorage("preferredColorScheme") private var preferredColorScheme = "system"
    ...
```

**Збережи:** `Cmd + S`

---

# КРОК 9: Запустити і перевірити

## 9.1 Build проект

1. В Xcode натисни **Cmd + B** (Build)

2. **Чекай ~1 хвилину** поки Firebase SDK компілюється (перший раз довго!)

3. Якщо є **помилки (червоні)** — напиши мені, виправимо

4. Якщо тільки **warnings (жовті)** — все ок, ігноруй

---

## 9.2 Запусти додаток

**⚠️ ВАЖЛИВО: Тест на РЕАЛЬНОМУ ПРИСТРОЇ!**

Push-повідомлення **НЕ ПРАЦЮЮТЬ** на симуляторі!

**Якщо маєш iPhone/iPad:**

1. Підключи до Mac

2. В Xcode вгорі вибери свій пристрій (не Simulator!)

3. Натисни **Cmd + R** (Run)

4. На пристрої може попросити дозволити:
   ```
   Trust this computer? → Trust
   ```

5. Додаток запуститься

**Якщо тільки симулятор:**

Все одно запусти — Firebase ініціалізується, але пуші не прийдуть.

---

## 9.3 Увімкни повідомлення в додатку

**В додатку:**

1. Перейди на таб **"Settings"** (Налаштування)

2. Scroll вниз до секції **"Notifications"**

3. Знайди **"Маркетингові повідомлення"**

4. Тапни → відкриється **"🔔 Push-повідомлення"**

5. Натисни **"Увімкнути повідомлення"**

6. В системному діалозі натисни **"Allow"** (Дозволити)

✅ Статус змінився на **"Увімкнено ✅"**

---

## 9.4 Тест локального повідомлення

**В тому ж екрані (Push-повідомлення):**

1. Scroll вниз до **"Developer Tools"** (тільки в DEBUG)

2. Натисни **"🧪 Тестове повідомлення (3 сек)"**

3. Згорни додаток (Home button / свайп вгору)

4. **Через 3 секунди** має прийти повідомлення:
   ```
   🎉 Нова версія!
   Оновіть додаток до версії 2.0
   ```

✅ Якщо прийшло — локальні пуші працюють!

---

## 9.5 Перевір FCM токен

**В тому ж екрані:**

1. В розділі **"Developer Tools"**

2. Має бути **"Token: ..."** (перші 20 символів)

3. Натисни **"📋 Скопіювати FCM токен"**

4. З'явиться alert **"Токен скопійовано"**

✅ Firebase працює! Токен згенеровано!

❌ Якщо **"FCM токен: очікування..."** → щось не так, повернись до Кроку 7.

---

# КРОК 10: Надіслати тестовий пуш з Firebase

## 10.1 Відкрий Firebase Console

**В браузері:**

1. Повернись на вкладку Firebase Console

2. Або зайди: **https://console.firebase.google.com**

3. Вибери проект **Track Habit**

---

## 10.2 Відкрий Cloud Messaging

1. В лівому меню натисни **"Messaging"** (або **"Engage → Messaging"**)

2. Натисни **"Send your first message"** (або **"New campaign"**)

3. Вибери **"Firebase Notification messages"**

---

## 10.3 Створи тестове повідомлення

**Notification:**

1. **Notification title:**
   ```
   🎉 Привіт з Firebase!
   ```

2. **Notification text:**
   ```
   Це перше тестове повідомлення!
   ```

3. (Опціонально) **Notification image:** Можна додати URL картинки

4. Натисни **"Send test message"** (праворуч)

---

## 10.4 Надішли на свій пристрій

1. Відкриється діалог **"Send test message"**

2. **Add an FCM registration token:**
   ```
   Вклей FCM токен з додатку (скопійований на кроці 9.5)
   ```

3. Натисни **"+"** (додати токен)

4. Токен з'явиться в списку

5. Натисни **"Test"** (внизу)

---

## 10.5 Перевір на пристрої

📱 **На iPhone/iPad має з'явитися повідомлення:**

```
🎉 Привіт з Firebase!
Це перше тестове повідомлення!
```

**Якщо додаток відкритий:**
- Побачиш banner вгорі

**Якщо додаток згорнутий:**
- Прийде на екран блокування

✅ **ПРАЦЮЄ!** Firebase push-повідомлення налаштовані!

---

# КРОК 11: Надіслати повідомлення ВСІМ користувачам

## 11.1 Створи кампанію

**В Firebase Console → Messaging:**

1. Натисни **"New campaign"**

2. **Notification:**
   ```
   Title: 🎉 Нова версія 2.0!
   Text: Оновіть додаток для нових фіч
   ```

3. Натисни **"Next"**

---

## 11.2 Налаштуй Target (Аудиторія)

1. **Target:**
   ```
   User segment: All users (Всі користувачі)
   
   Або вибери:
   iOS app → Track Habit
   ```

2. Натисни **"Next"**

---

## 11.3 Scheduling (Коли надіслати)

1. **Scheduling:**
   ```
   Now (Зараз)
   
   Або Schedule (Запланувати на потім)
   ```

2. Натисни **"Next"**

---

## 11.4 Додаткові опції (опціонально)

**Conversion events:** Пропусти

**Additional options:**
- **Sound:** Default
- **Priority:** High

Натисни **"Review"**

---

## 11.5 Опублікуй кампанію

1. Перевір всі дані

2. Натисни **"Publish"** (Опублікувати)

✅ Повідомлення надіслано всім користувачам!

---

# 🎉 ГОТОВО!

## Що ти можеш зараз:

✅ Надсилати push-повідомлення всім юзерам  
✅ Планувати кампанії на потім  
✅ Сегментувати аудиторію  
✅ Дивитись аналітику (скільки доставлено, відкрито)  
✅ Безкоштовно, без лімітів!  

---

## Типи повідомлень які можеш слати:

### 🆕 Оновлення версії
```
Title: 🎉 Нова версія 2.1!
Text: Оновіть додаток для нових фіч та виправлень
```

### ✨ Нова фіча
```
Title: ✨ Нова фіча: Досягнення!
Text: Розблоковуй бейджі за свої успіхи 🏆
```

### 🎁 Акція
```
Title: 🎁 -50% на Pro!
Text: Тільки сьогодні безліміт звичок зі знижкою
```

### 🔥 Мотиваційне
```
Title: 🔥 Не забувай!
Text: Твої звички чекають на тебе сьогодні
```

---

## 📊 Де дивитись статистику?

**Firebase Console → Messaging → Campaigns:**

- 📈 **Sent:** Скільки надіслано
- 📬 **Delivered:** Скільки доставлено
- 👆 **Opened:** Скільки відкрито
- ⏰ **Best time:** Найкращий час для надсилання

---

## ❓ Якщо щось не працює:

### Повідомлення не приходять:

1. ✅ Перевір що `GoogleService-Info.plist` в проекті
2. ✅ Перевір Bundle ID (Xcode === Firebase)
3. ✅ Перевір що APNs ключ (.p8) завантажений в Firebase
4. ✅ Тестуй на **реальному пристрої**, не симуляторі!
5. ✅ Перевір що **Push Notifications** capability додана
6. ✅ Перевір що юзер дозволив пуші в системному діалозі

### FCM токен не генерується:

1. Перевір консоль Xcode на помилки (⌘ + 8)
2. Перевір що `FirebaseApp.configure()` викликається
3. Перезапусти додаток

### Помилка компіляції:

Напиши мені — виправимо разом!

---

# 💰 Вартість:

## Firebase Cloud Messaging:
- ✅ **БЕЗКОШТОВНО назавжди**
- ♾️ Необмежена кількість повідомлень
- 👥 Необмежена кількість користувачів
- 📊 Аналітика включена

## Apple Developer Account:
- 💵 **$99/рік** (потрібен для публікації в App Store)
- Push-повідомлення працюють тільки з платним акаунтом
- Для локального тестування можна без нього

---

🚀 **Насолоджуйся!** Тепер ти можеш тримати зв'язок з юзерами! 🎉

Питання? Пиши мені! 💬

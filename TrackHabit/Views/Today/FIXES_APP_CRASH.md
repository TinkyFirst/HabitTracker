# 🔧 Виправлення помилок запуску додатку

## ❌ Проблеми, які були:

### 1. **SwiftData міграція**
- Додали нові поля `dailyRepetitions` та `reminderTimes` без значень за замовчуванням
- SwiftData не міг мігрувати існуючі об'єкти Habit

### 2. **Відсутній View Extension**
- Видалили `extension View` з HabitDetailView та InsightsView
- Створили ViewExtensions.swift, але файл не був доданий до проекту Xcode
- Компілятор не міг знайти метод `.if()`

### 3. **Відсутній import Foundation**
- У Habit.swift не було `import Foundation`
- Це могло викликати проблеми з типами Date, UUID і т.д.

## ✅ Виправлення:

### 1. **Habit.swift**
```swift
// ДО (не працювало):
var dailyRepetitions: Int
var reminderTimes: [Date]?

// ПІСЛЯ (працює):
var dailyRepetitions: Int = 1
var reminderTimes: [Date]? = nil
```

**Пояснення:**
- `= 1` - значення за замовчуванням для існуючих звичок
- `= nil` - опціональне поле для міграції

### 2. **View Extension**
Повернули extension в `HabitDetailView.swift` з коментарем:
```swift
// MARK: - View Extension for Conditional Modifiers
// Note: This extension is used across multiple files
// Placed here to avoid duplication errors
extension View {
    @ViewBuilder
    func `if`<Transform: View>(_ condition: Bool, transform: (Self) -> Transform) -> some View {
        if condition {
            transform(self)
        } else {
            self
        }
    }
}
```

### 3. **Додано import Foundation**
```swift
import Foundation
import SwiftData
```

## 📝 Що треба зробити в Xcode:

### Якщо додаток все ще не запускається:

1. **Очистити build folder:**
   - Product → Clean Build Folder (⇧⌘K)

2. **Видалити Derived Data:**
   - Xcode → Settings → Locations → Derived Data → Delete

3. **Перезапустити симулятор:**
   - Закрити симулятор
   - Restart симулятора

4. **Видалити додаток з симулятора:**
   - Довге натискання на іконку → Remove App
   - Це видалить стару базу даних

5. **Якщо є фізичний пристрій:**
   - Видалити додаток
   - Reinstall

## 🎯 Перевірка:

Після цих кроків додаток повинен:
- ✅ Запуститися без crash
- ✅ Показати існуючі звички з `dailyRepetitions = 1`
- ✅ Дозволити створювати нові звички
- ✅ Редагувати кількість виконань

## ⚠️ На майбутнє:

Для додавання нових полів до SwiftData моделі завжди:
1. Додавати значення за замовчуванням (`= value`)
2. Робити поля опціональними (`Type?`)
3. Або використовувати міграційні схеми SwiftData

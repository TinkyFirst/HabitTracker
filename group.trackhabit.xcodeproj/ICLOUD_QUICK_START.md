# 🚀 Швидка інструкція: iCloud синхронізація

## 📱 Основні кроки (швидка версія)

### 1. Налаштування головного додатку (TrackHabit)
```
Проект → Target: TrackHabit → Signing & Capabilities:
✅ CloudKit (вибрати: iCloud.com.andrii.trackhabit)
✅ App Groups (вибрати: group.com.trackhabit.shared)
```

### 2. Налаштування Widget Extension (HabitWidgetExtension)
```
Проект → Target: HabitWidgetExtension → Signing & Capabilities:
✅ Додати iCloud → CloudKit (вибрати: iCloud.com.andrii.trackhabit)
✅ App Groups (вибрати: group.com.trackhabit.shared)
```

### 3. Перевірити SharedModelContainer.swift
```swift
let appGroupIdentifier = "group.com.trackhabit.shared" // ← має збігатися!
```

### 4. Додати файл до таргетів
```
SharedModelContainer.swift → File Inspector → Target Membership:
✅ TrackHabit
✅ HabitWidgetExtension
```

### 5. Очистити та зібрати
```
Product → Clean Build Folder (⇧⌘K)
Product → Build (⌘B)
```

---

## ✅ Чеклист перевірки

- [ ] CloudKit додано до TrackHabit target
- [ ] CloudKit додано до HabitWidgetExtension target
- [ ] Той самий CloudKit Container в обох
- [ ] Та сама App Group в обох
- [ ] App Group identifier в коді збігається
- [ ] SharedModelContainer.swift в обох таргетах
- [ ] Проект збирається без помилок

---

## 🧪 Швидке тестування

1. Запустіть додаток (⌘R)
2. Додайте звичку
3. Перевірте консоль: має бути "☁️ CloudKit sync: enabled"
4. Додайте віджет на домашній екран
5. Звичка має відобразитися у віджеті

---

## 🔥 Якщо щось не працює

### Помилка збірки:
```bash
Product → Clean Build Folder (⇧⌘K)
Перезапустіть Xcode
```

### Віджет не показує дані:
```bash
1. Перевірити Target Membership для SharedModelContainer.swift
2. Перевірити App Group в обох таргетах
3. Видалити та додати віджет знову
```

### Не синхронізується між пристроями:
```bash
1. Увійти в той самий Apple ID
2. Увімкнути iCloud Drive
3. Використовувати РЕАЛЬНІ пристрої (не симулятори!)
```

---

## 📞 Перевірка статусу iCloud

Додайте в налаштування додатку:

```swift
import SwiftUI

struct SettingsView: View {
    var body: some View {
        List {
            Section("Синхронізація") {
                NavigationLink("Статус iCloud") {
                    CloudKitStatusView()
                }
            }
        }
    }
}
```

---

## 📊 Моніторинг в консолі

При запуску додатку шукайте:
```
✅ SharedModelContainer initialized successfully
☁️ CloudKit sync: enabled
```

---

## ⏱️ Час синхронізації

- Локально (між додатком і віджетом): миттєво
- Між пристроями: 10-30 секунд
- При поганому інтернеті: до 1-2 хвилин

---

**Детальна інструкція:** ICLOUD_SETUP_INSTRUCTIONS.md
